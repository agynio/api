type: object
properties:
  items:
    type: array
    items: { $ref: './Chat.yaml' }
  nextPageToken:
    type: string
    description: Opaque cursor for the next page. Absent when there are no more results.
required: [items]
