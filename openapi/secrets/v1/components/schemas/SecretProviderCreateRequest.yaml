type: object
properties:
  title: { type: string }
  description: { type: string }
  type: { $ref: './SecretProviderType.yaml' }
  config: { $ref: './SecretProviderConfig.yaml' }
required: [type, config]
