type: object
properties:
  title: { type: string }
  description: { type: string }
  secretProviderId: { type: string, format: uuid }
  remoteName: { type: string }
required: [secretProviderId, remoteName]
