post:
  tags: [Secrets]
  summary: Resolve secret value
  parameters:
    - $ref: '../components/parameters/IdPath.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/ResolvedSecretValue.yaml'
    default:
      $ref: '../components/responses/ProblemResponse.yaml'
