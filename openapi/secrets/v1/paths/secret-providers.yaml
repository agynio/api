get:
  tags: [SecretProviders]
  summary: List secret providers
  parameters:
    - in: query
      name: page
      schema:
        type: integer
        minimum: 1
        default: 1
    - in: query
      name: perPage
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/PaginatedSecretProviders.yaml'
    default:
      $ref: '../components/responses/ProblemResponse.yaml'
post:
  tags: [SecretProviders]
  summary: Create secret provider
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/SecretProviderCreateRequest.yaml'
  responses:
    '201':
      description: Created
      content:
        application/json:
          schema:
            $ref: '../components/schemas/SecretProvider.yaml'
    default:
      $ref: '../components/responses/ProblemResponse.yaml'
