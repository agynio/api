allOf:
  - $ref: './EntityMeta.yaml'
  - type: object
    properties:
      title: { type: string }
      name: { type: string }
      role: { type: string }
      config: { $ref: './AgentConfig.yaml' }
    required: [config]
