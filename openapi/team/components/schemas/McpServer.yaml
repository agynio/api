allOf:
  - $ref: './EntityMeta.yaml'
  - type: object
    properties:
      title: { type: string }
      namespace: { type: string }
      config: { $ref: './McpServerConfig.yaml' }
    required: [config]
