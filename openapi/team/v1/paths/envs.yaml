get:
  tags: [Envs]
  summary: List envs
  parameters:
    - in: query
      name: pageSize
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    - in: query
      name: pageToken
      schema:
        type: string
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/PaginatedEnvs.yaml'
    default:
      $ref: '../components/responses/ProblemResponse.yaml'
post:
  tags: [Envs]
  summary: Create env
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/EnvCreateRequest.yaml'
  responses:
    '201':
      description: Created
      content:
        application/json:
          schema:
            $ref: '../components/schemas/Env.yaml'
    default:
      $ref: '../components/responses/ProblemResponse.yaml'
