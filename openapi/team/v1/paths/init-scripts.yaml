get:
  tags: [Init Scripts]
  summary: List init scripts
  parameters:
    - in: query
      name: pageSize
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    - in: query
      name: pageToken
      schema:
        type: string
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../components/schemas/PaginatedInitScripts.yaml'
    default:
      $ref: '../components/responses/ProblemResponse.yaml'
post:
  tags: [Init Scripts]
  summary: Create init script
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/InitScriptCreateRequest.yaml'
  responses:
    '201':
      description: Created
      content:
        application/json:
          schema:
            $ref: '../components/schemas/InitScript.yaml'
    default:
      $ref: '../components/responses/ProblemResponse.yaml'
